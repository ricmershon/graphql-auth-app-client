/**
 * @file    Contains component for navigation errors
 * @author  Ric Mershon
 */

/**
 * PageNotFound: App lands here when user attempts to navigate to a route that
 * does not exist.
 * 
 * @returns an error message.
 */

const PageNotFound = () => (
    <h1>This page does not exist</h1>
)

export default PageNotFound